# Note importante
Le contenu de cet essai est codé en dur directement dans les fichiers .astro (src/pages/ et src/layouts/), PAS dans des fichiers Markdown.
Pour modifier du texte, cherche directement dans src/ avec : grep -r 'ton texte' src/
